---
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Navigation.jsx';
---

<Layout title="Azorean Studios - Creative Technology Lab">
  <Navigation client:load />
  
  <main>
    <!-- Hero Section -->
    <section class="hero">
      <div class="container">
        <div class="hero-content">
          <span class="hero-label">AZOREAN STUDIOS</span>
          <h1>Creative Technology Lab</h1>
          <p class="hero-description">
            The digital laboratory where we experiment with mindful technology, 
            building tools and experiences that serve human consciousness and connection.
          </p>
        </div>
      </div>
    </section>

    <!-- Mission Section -->
    <section class="mission">
      <div class="container">
        <div class="mission-content">
          <div class="mission-text">
            <h2>Our Mission</h2>
            <p>
              As the technological backbone of Azorean Studios, we develop innovative 
              digital solutions that prioritize human wellbeing and authentic engagement. 
              Our work spans interactive applications, mindfulness tools, and experimental 
              interfaces that bridge the digital and physical realms.
            </p>
            <p>
              Every line of code we write is intentional, every interaction we design 
              is meaningful, and every tool we build is crafted to enhance rather than 
              diminish human potential.
            </p>
          </div>
          <div class="mission-visual">
            <div class="code-animation">
              <div class="code-line"></div>
              <div class="code-line"></div>
              <div class="code-line"></div>
              <div class="code-line"></div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Focus Areas -->
    <section class="focus-areas">
      <div class="container">
        <h2 class="section-title">Focus Areas</h2>
        
        <div class="areas-grid">
          <div class="area-card">
            <div class="area-icon">
              <svg width="48" height="48" viewBox="0 0 48 48" fill="none">
                <path d="M24 4L34 14L24 24L14 14L24 4Z" stroke="currentColor" stroke-width="2"/>
                <path d="M4 24L14 34L24 24L14 14L4 24Z" stroke="currentColor" stroke-width="2"/>
                <path d="M44 24L34 34L24 24L34 14L44 24Z" stroke="currentColor" stroke-width="2"/>
                <path d="M24 44L34 34L24 24L14 34L24 44Z" stroke="currentColor" stroke-width="2"/>
              </svg>
            </div>
            <h3>Mindful Applications</h3>
            <p>Interactive tools designed to support meditation, reflection, and conscious living through intuitive digital interfaces.</p>
          </div>
          
          <div class="area-card">
            <div class="area-icon">
              <svg width="48" height="48" viewBox="0 0 48 48" fill="none">
                <circle cx="24" cy="24" r="20" stroke="currentColor" stroke-width="2"/>
                <path d="M24 8V24L32 32" stroke="currentColor" stroke-width="2"/>
                <circle cx="24" cy="24" r="2" fill="currentColor"/>
              </svg>
            </div>
            <h3>Experimental Interfaces</h3>
            <p>Pushing the boundaries of human-computer interaction with gesture-based, voice-activated, and ambient interfaces.</p>
          </div>
          
          <div class="area-card">
            <div class="area-icon">
              <svg width="48" height="48" viewBox="0 0 48 48" fill="none">
                <path d="M8 24C8 15.163 15.163 8 24 8C32.837 8 40 15.163 40 24C40 32.837 32.837 40 24 40" stroke="currentColor" stroke-width="2"/>
                <path d="M8 24C8 32.837 15.163 40 24 40" stroke="currentColor" stroke-width="2" stroke-dasharray="4 4"/>
                <circle cx="24" cy="24" r="4" stroke="currentColor" stroke-width="2"/>
              </svg>
            </div>
            <h3>Digital Wellness</h3>
            <p>Creating technology that promotes healthy digital habits and supports users in maintaining balance and presence.</p>
          </div>
          
          <div class="area-card">
            <div class="area-icon">
              <svg width="48" height="48" viewBox="0 0 48 48" fill="none">
                <path d="M12 36L24 24L36 36M24 24L36 12M24 24L12 12" stroke="currentColor" stroke-width="2"/>
                <circle cx="24" cy="24" r="2" fill="currentColor"/>
              </svg>
            </div>
            <h3>Community Platforms</h3>
            <p>Building digital spaces that facilitate authentic connection and meaningful collaboration between creators and communities.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Technology Stack -->
    <section class="tech-stack">
      <div class="container">
        <div class="stack-content">
          <div class="stack-text">
            <span class="section-label">Our Tools</span>
            <h2>Technology Stack</h2>
            <p>
              We leverage cutting-edge technologies while maintaining our commitment to 
              privacy, performance, and user-centered design. Our stack is carefully 
              chosen to support rapid prototyping and sustainable development.
            </p>
            
            <div class="tech-categories">
              <div class="tech-category">
                <h4>Frontend</h4>
                <p>React, Astro, Three.js, WebGL</p>
              </div>
              <div class="tech-category">
                <h4>Backend</h4>
                <p>Node.js, Python, GraphQL, REST APIs</p>
              </div>
              <div class="tech-category">
                <h4>Audio</h4>
                <p>Web Audio API, Tone.js, Max/MSP</p>
              </div>
              <div class="tech-category">
                <h4>AI/ML</h4>
                <p>TensorFlow, PyTorch, OpenAI APIs</p>
              </div>
            </div>
          </div>
          
          <div class="stack-visual">
            <div class="floating-tech">
              <div class="tech-orbit">
                <div class="tech-node"></div>
                <div class="tech-node"></div>
                <div class="tech-node"></div>
                <div class="tech-node"></div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Call to Action -->
    <section class="cta">
      <div class="container">
        <div class="cta-content">
          <h2>Ready to Collaborate?</h2>
          <p>Let's explore how mindful technology can serve your vision.</p>
          <a href="/contact" class="cta-button">Start a Conversation</a>
        </div>
      </div>
    </section>
  </main>
</Layout>

<style>
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .hero {
    padding: 8rem 0 4rem;
    background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
    text-align: center;
  }

  .hero-label {
    display: inline-block;
    font-size: 0.875rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--color-accent);
    margin-bottom: 1.5rem;
  }

  .hero h1 {
    margin-bottom: 2rem;
    color: var(--color-primary);
  }

  .hero-description {
    font-size: 1.25rem;
    max-width: 600px;
    margin: 0 auto;
  }

  .mission {
    padding: var(--spacing-3xl) 0;
  }

  .mission-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .mission-text h2 {
    margin-bottom: 2rem;
    color: var(--color-primary);
  }

  .mission-text p {
    margin-bottom: 1.5rem;
    font-size: 1.125rem;
  }

  .mission-visual {
    display: flex;
    align-items: center;
    justify-content: center;
    height: 300px;
  }

  .code-animation {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .code-line {
    height: 8px;
    background: linear-gradient(90deg, var(--color-accent), transparent);
    border-radius: 4px;
    animation: code-flow 2s ease-in-out infinite;
  }

  .code-line:nth-child(1) { width: 200px; animation-delay: 0s; }
  .code-line:nth-child(2) { width: 150px; animation-delay: 0.5s; }
  .code-line:nth-child(3) { width: 180px; animation-delay: 1s; }
  .code-line:nth-child(4) { width: 120px; animation-delay: 1.5s; }

  @keyframes code-flow {
    0%, 100% { opacity: 0.3; }
    50% { opacity: 1; }
  }

  .focus-areas {
    padding: var(--spacing-3xl) 0;
    background: var(--color-bg-soft);
  }

  .section-title {
    text-align: center;
    margin-bottom: 4rem;
    color: var(--color-primary);
  }

  .section-label {
    display: inline-block;
    font-size: 0.875rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--color-accent);
    margin-bottom: 1rem;
  }

  .areas-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
  }

  .area-card {
    background: white;
    padding: 2.5rem;
    border-radius: 1rem;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
    transition: var(--transition);
    text-align: center;
  }

  .area-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 12px 40px rgba(74, 144, 164, 0.15);
  }

  .area-icon {
    color: var(--color-accent);
    margin-bottom: 1.5rem;
  }

  .area-card h3 {
    margin-bottom: 1rem;
    color: var(--color-primary);
  }

  .area-card p {
    font-size: 1rem;
    line-height: 1.6;
  }

  .tech-stack {
    padding: var(--spacing-3xl) 0;
  }

  .stack-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .stack-text h2 {
    margin-bottom: 2rem;
    color: var(--color-primary);
  }

  .stack-text p {
    margin-bottom: 2rem;
    font-size: 1.125rem;
  }

  .tech-categories {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
  }

  .tech-category h4 {
    color: var(--color-accent);
    margin-bottom: 0.5rem;
    font-size: 1rem;
  }

  .tech-category p {
    font-size: 0.9rem;
    color: var(--color-text-light);
    margin: 0;
  }

  .stack-visual {
    display: flex;
    align-items: center;
    justify-content: center;
    height: 300px;
  }

  .floating-tech {
    position: relative;
    width: 200px;
    height: 200px;
  }

  .tech-orbit {
    position: relative;
    width: 100%;
    height: 100%;
    border: 2px solid var(--color-border);
    border-radius: 50%;
    animation: orbit 20s linear infinite;
  }

  .tech-node {
    position: absolute;
    width: 12px;
    height: 12px;
    background: var(--color-accent);
    border-radius: 50%;
  }

  .tech-node:nth-child(1) { top: -6px; left: 50%; transform: translateX(-50%); }
  .tech-node:nth-child(2) { right: -6px; top: 50%; transform: translateY(-50%); }
  .tech-node:nth-child(3) { bottom: -6px; left: 50%; transform: translateX(-50%); }
  .tech-node:nth-child(4) { left: -6px; top: 50%; transform: translateY(-50%); }

  @keyframes orbit {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  .cta {
    padding: var(--spacing-3xl) 0;
    background: var(--color-primary);
    color: white;
    text-align: center;
  }

  .cta-content h2 {
    margin-bottom: 1rem;
    color: white;
  }

  .cta-content p {
    margin-bottom: 2rem;
    font-size: 1.25rem;
    color: rgba(255, 255, 255, 0.8);
  }

  .cta-button {
    display: inline-block;
    padding: 1rem 2rem;
    background: var(--color-accent);
    color: white;
    text-decoration: none;
    border-radius: var(--border-radius);
    font-weight: 600;
    font-size: 1.125rem;
    transition: var(--transition);
  }

  .cta-button:hover {
    background: white;
    color: var(--color-primary);
    transform: translateY(-2px);
  }

  @media (max-width: 768px) {
    .hero {
      padding: 6rem 0 3rem;
    }

    .mission-content,
    .stack-content {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .mission-visual,
    .stack-visual {
      height: 200px;
    }

    .tech-categories {
      grid-template-columns: 1fr;
    }

    .areas-grid {
      grid-template-columns: 1fr;
    }
  }
</style> 