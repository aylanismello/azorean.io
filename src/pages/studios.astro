---
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Navigation.jsx';
---

<Layout title="Studios">
  <Navigation client:load />
  
  <main>
    <section class="studios">
      <div class="container">
        <h1>Studios</h1>
        
        <div class="content">
          <p class="intro">
            We build tools at the intersection of wellness, sound, and embodiment.
          </p>
          
          <p class="description">
            Small, intentional apps — often experimental — that explore how technology can support nervous system regulation, creative flow, and deeper presence.
          </p>
          
          <div class="focus-areas">
            <div class="area">Somatic tools that encourage breath, awareness, and stillness</div>
            <div class="area">Audio-based experiments that use generative sound or ambient environments</div>
            <div class="area">Interactive web projects inspired by nature, the ocean, and human rhythm</div>
          </div>
          
          <p class="philosophy">
            We don't chase scale.<br />
            We build for feeling.
          </p>
          
          <p class="closing">
            Each project reflects a commitment to slowness, simplicity, and intuitive design — shaped by our roots in creative coding, music, and embodied experience.
          </p>
        </div>
      </div>
    </section>
  </main>
</Layout>

<style>
  .container {
    max-width: 1000px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .studios {
    padding: 8rem 0 6rem;
    min-height: 85vh;
  }

  h1 {
    font-size: clamp(2.5rem, 8vw, 6rem);
    font-weight: 700;
    margin-bottom: 4rem;
    color: var(--color-primary);
    line-height: 0.9;
    letter-spacing: -0.04em;
  }

  .content {
    max-width: 800px;
  }

  .intro {
    font-size: clamp(1.5rem, 4vw, 2.5rem);
    font-weight: 600;
    margin-bottom: 3rem;
    color: var(--color-primary);
    line-height: 1.2;
    letter-spacing: -0.02em;
  }

  .description {
    font-size: clamp(1.125rem, 2.5vw, 1.375rem);
    line-height: 1.6;
    margin-bottom: 3rem;
    color: var(--color-text-light);
    font-weight: 400;
  }

  .focus-areas {
    margin: 3rem 0 4rem;
  }

  .area {
    font-size: clamp(1rem, 2.2vw, 1.25rem);
    line-height: 1.6;
    margin-bottom: 1.5rem;
    color: var(--color-text);
    font-weight: 400;
    padding-left: 1.5rem;
    position: relative;
  }

  .area::before {
    content: '•';
    position: absolute;
    left: 0;
    color: var(--color-accent);
    font-weight: 600;
  }

  .philosophy {
    font-size: clamp(1.75rem, 4vw, 2.75rem);
    font-weight: 600;
    color: var(--color-accent);
    margin: 4rem 0;
    line-height: 1.2;
    letter-spacing: -0.02em;
  }

  .closing {
    font-size: clamp(1.125rem, 2.5vw, 1.375rem);
    line-height: 1.6;
    color: var(--color-text-light);
    font-weight: 400;
    margin-bottom: 0;
  }

  @media (max-width: 768px) {
    .studios {
      padding: 6rem 0 4rem;
    }
    
    .container {
      padding: 0 1rem;
    }

    h1 {
      margin-bottom: 3rem;
    }

    .intro {
      margin-bottom: 2rem;
    }

    .description {
      margin-bottom: 2rem;
    }

    .focus-areas {
      margin: 2rem 0 3rem;
    }

    .philosophy {
      margin: 3rem 0;
    }
  }

  @media (max-width: 480px) {
    .area {
      padding-left: 1rem;
    }
  }
</style> 