---
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Navigation.jsx';
import Hero from '../components/Hero.jsx';
---

<Layout title="Azorean Studios - Creative Technology & Mindful Design">
  <Navigation client:load />
  <Hero client:load />
  
  <main>
    <!-- Overview Section -->
    <section class="overview">
      <div class="container">
        <div class="overview-content">
          <div class="overview-text">
            <h2>Bridging Technology & Consciousness</h2>
            <p>
              Azorean Studios operates at the intersection of cutting-edge technology 
              and mindful design, creating digital experiences that resonate on both 
              intellectual and emotional levels.
            </p>
            <p>
              Through our three core divisions, we explore sound, community, and 
              technological innovation as pathways to deeper human connection.
            </p>
          </div>
          <div class="overview-visual">
            <div class="floating-element"></div>
            <div class="floating-element"></div>
            <div class="floating-element"></div>
          </div>
        </div>
      </div>
    </section>

    <!-- Divisions Section -->
    <section class="divisions">
      <div class="container">
        <h2 class="section-title">Our Creative Divisions</h2>
        
        <div class="divisions-grid">
          <div class="division-card">
            <div class="card-icon">
              <svg width="40" height="40" viewBox="0 0 40 40" fill="none">
                <path d="M35 20C35 28.284 28.284 35 20 35C11.716 35 5 28.284 5 20C5 11.716 11.716 5 20 5C28.284 5 35 11.716 35 20Z" stroke="currentColor" stroke-width="2"/>
                <path d="M15 15L25 20L15 25V15Z" fill="currentColor"/>
              </svg>
            </div>
            <h3>Azorean Studios</h3>
            <p>The tech and product arm powering our digital lab of mindful tools and experiences.</p>
            <a href="/studios" class="card-link">Explore Technology →</a>
          </div>
          
          <div class="division-card">
            <div class="card-icon">
              <svg width="40" height="40" viewBox="0 0 40 40" fill="none">
                <path d="M20 5L25 15H35L27.5 22.5L30 32.5L20 27.5L10 32.5L12.5 22.5L5 15H15L20 5Z" stroke="currentColor" stroke-width="2"/>
              </svg>
            </div>
            <h3>Segundo Sol</h3>
            <p>Our label and collective focusing on community, intentional gatherings, and music releases.</p>
            <a href="/segundo-sol" class="card-link">Discover Community →</a>
          </div>
          
          <div class="division-card">
            <div class="card-icon">
              <svg width="40" height="40" viewBox="0 0 40 40" fill="none">
                <path d="M8 12H32L28 32H12L8 12Z" stroke="currentColor" stroke-width="2"/>
                <path d="M8 12L6 4H2" stroke="currentColor" stroke-width="2"/>
                <circle cx="13" cy="37" r="1" fill="currentColor"/>
                <circle cx="27" cy="37" r="1" fill="currentColor"/>
                <path d="M20 20V24M16 22H24" stroke="currentColor" stroke-width="2"/>
              </svg>
            </div>
            <h3>Azorean Sound</h3>
            <p>Our modular recording studio and audio lab for sound design, ambient works, and audio R&D.</p>
            <a href="/sound" class="card-link">Enter the Studio →</a>
          </div>
        </div>
      </div>
    </section>

    <!-- Philosophy Section -->
    <section class="philosophy">
      <div class="container">
        <div class="philosophy-content">
          <div class="philosophy-text">
            <span class="section-label">Our Philosophy</span>
            <h2>Mindful Innovation</h2>
            <p>
              We believe technology should serve human flourishing. Every project we undertake 
              is guided by principles of consciousness, sustainability, and authentic connection.
            </p>
            <p>
              From ambient soundscapes to digital experiences, we craft tools that invite 
              presence and foster genuine community.
            </p>
          </div>
          <div class="philosophy-visual">
            <div class="ripple-animation">
              <div class="ripple"></div>
              <div class="ripple"></div>
              <div class="ripple"></div>
            </div>
          </div>
        </div>
      </div>
    </section>
  </main>
</Layout>

<style>
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .overview {
    padding: var(--spacing-3xl) 0;
    background: var(--color-bg-soft);
  }

  .overview-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .overview-text h2 {
    margin-bottom: 2rem;
    color: var(--color-primary);
  }

  .overview-text p {
    margin-bottom: 1.5rem;
    font-size: 1.125rem;
  }

  .overview-text p:last-child {
    margin-bottom: 0;
  }

  .overview-visual {
    position: relative;
    height: 300px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .floating-element {
    position: absolute;
    width: 60px;
    height: 60px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--color-accent), rgba(74, 144, 164, 0.3));
    animation: float 6s ease-in-out infinite;
  }

  .floating-element:nth-child(1) {
    top: 20%;
    left: 20%;
    animation-delay: 0s;
  }

  .floating-element:nth-child(2) {
    top: 60%;
    right: 30%;
    animation-delay: 2s;
  }

  .floating-element:nth-child(3) {
    bottom: 20%;
    left: 50%;
    animation-delay: 4s;
  }

  @keyframes float {
    0%, 100% { transform: translateY(0) scale(1); }
    50% { transform: translateY(-20px) scale(1.1); }
  }

  .divisions {
    padding: var(--spacing-3xl) 0;
  }

  .section-title {
    text-align: center;
    margin-bottom: 4rem;
    color: var(--color-primary);
  }

  .section-label {
    display: inline-block;
    font-size: 0.875rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--color-accent);
    margin-bottom: 1rem;
  }

  .divisions-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }

  .division-card {
    background: white;
    padding: 2.5rem;
    border-radius: 1rem;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
    transition: var(--transition);
    border: 1px solid var(--color-border);
  }

  .division-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 12px 40px rgba(74, 144, 164, 0.15);
  }

  .card-icon {
    color: var(--color-accent);
    margin-bottom: 1.5rem;
  }

  .division-card h3 {
    margin-bottom: 1rem;
    color: var(--color-primary);
  }

  .division-card p {
    margin-bottom: 2rem;
    font-size: 1rem;
  }

  .card-link {
    color: var(--color-accent);
    text-decoration: none;
    font-weight: 600;
    transition: var(--transition);
  }

  .card-link:hover {
    color: var(--color-primary);
  }

  .philosophy {
    padding: var(--spacing-3xl) 0;
    background: var(--color-bg-soft);
  }

  .philosophy-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .philosophy-text h2 {
    margin-bottom: 2rem;
    color: var(--color-primary);
  }

  .philosophy-text p {
    margin-bottom: 1.5rem;
    font-size: 1.125rem;
  }

  .philosophy-visual {
    display: flex;
    align-items: center;
    justify-content: center;
    height: 300px;
  }

  .ripple-animation {
    position: relative;
    width: 200px;
    height: 200px;
  }

  .ripple {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    border: 2px solid var(--color-accent);
    border-radius: 50%;
    animation: ripple 3s linear infinite;
  }

  .ripple:nth-child(1) { animation-delay: 0s; }
  .ripple:nth-child(2) { animation-delay: 1s; }
  .ripple:nth-child(3) { animation-delay: 2s; }

  @keyframes ripple {
    0% {
      width: 0;
      height: 0;
      opacity: 1;
    }
    100% {
      width: 200px;
      height: 200px;
      opacity: 0;
    }
  }

  @media (max-width: 768px) {
    .overview-content,
    .philosophy-content {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .overview-visual,
    .philosophy-visual {
      height: 200px;
    }

    .divisions-grid {
      grid-template-columns: 1fr;
    }
  }
</style> 