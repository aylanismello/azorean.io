---
// No imports from existing components to keep this page completely separate
---

<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Fluindo - Electronic Press Kit</title>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600&display=swap"
            rel="stylesheet"
        />
    </head>
    <body>
        <div class="fluid-bg">
            <div class="wave wave-1"></div>
            <div class="wave wave-2"></div>
            <div class="wave wave-3"></div>
        </div>

        <div class="epk-container">
            <!-- Hero Section -->
            <section class="hero-section">
                <div class="hero-background">
                    <img src="/hy.jpeg" alt="Fluindo" class="hero-image" />
                </div>
                <div class="hero-content">
                    <div class="hero-text">
                        <h1 class="artist-name">Fluindo</h1>
                        <p class="tagline">Future Bossa • Global Downtempo</p>
                        <p class="subtitle">FKA Hooded Youth</p>
                    </div>
                </div>
            </section>

            <!-- Bio Section -->
            <section class="bio-section">
                <div class="section-header">
                    <h2 class="section-title">About</h2>
                    <div class="section-line"></div>
                </div>
                <div class="bio-content">
                    <p>
                        <strong>Fluindo</strong> is an LA-based/native electronic
                        artist blending bossa nova, leftfield club, and organic downtempo.
                        Their sound merges intricate rhythm with textured atmospheres,
                        drawing from field recordings and generative processes. Rooted
                        in both warmth and precision, Fluindo creates music that
                        moves between intimacy and expanse.
                    </p>
                </div>

                <!-- Music Section -->
                <section class="music-section">
                    <div class="section-header">
                        <h2 class="section-title">Music</h2>
                        <div class="section-line"></div>
                    </div>

                    <div class="music-content">
                        <!-- Featured Spotify Track -->
                        <div class="spotify-embed">
                            <iframe
                                style="border-radius:12px"
                                src="https://open.spotify.com/embed/track/7hLHksy2SRLn9LoeWv5m7J?utm_source=generator&theme=0"
                                width="100%"
                                height="152"
                                frameborder="0"
                                allowfullscreen=""
                                allow="autoplay; clipboard-write; encrypted-media; fullscreen; picture-in-picture"
                                loading="lazy"></iframe>
                        </div>

                        <div class="platform-links">
                            <a
                                href="https://open.spotify.com/artist/1Qu1p309YeihUAgSe2FXDQ?si=xHq61LvmSWe9Yamz1p2vgw"
                                target="_blank"
                                class="platform-link">Spotify</a
                            >
                            <a
                                href="https://soundcloud.com/fluindo"
                                target="_blank"
                                class="platform-link">SoundCloud</a
                            >
                            <a
                                href="https://www.instagram.com/f.luindo/"
                                target="_blank"
                                class="platform-link">Instagram</a
                            >
                        </div>
                    </div>
                </section>

                <!-- Contact Section -->
                <section class="contact-section">
                    <div class="contact-content">
                        <div class="section-header">
                            <h2 class="section-title">Connect</h2>
                            <div class="section-line"></div>
                        </div>
                        <p class="contact-intro">
                            For bookings, collaborations, or conversations about
                            music and presence
                        </p>

                        <div class="contact-grid">
                            <div class="contact-card">
                                <h3>Fluindo</h3>
                                <a
                                    href="mailto:fluindo@azorean.io"
                                    class="contact-link">fluindo@azorean.io</a
                                >
                                <p>
                                    Official project contact for bookings,
                                    collaborations, and all Fluindo-related
                                    inquiries
                                </p>
                            </div>

                            <div class="contact-card">
                                <h3>Aylan Mello</h3>
                                <a
                                    href="mailto:aylanismello@gmail.com"
                                    class="contact-link"
                                    >aylanismello@gmail.com</a
                                >
                                <p>
                                    Personal contact for direct communication
                                    and artistic discussions
                                </p>
                            </div>
                        </div>

                        <div class="social-platforms">
                            <a
                                href="https://open.spotify.com/artist/1Qu1p309YeihUAgSe2FXDQ?si=xHq61LvmSWe9Yamz1p2vgw"
                                target="_blank"
                                class="social-link">Spotify</a
                            >
                            <a
                                href="https://soundcloud.com/fluindo"
                                target="_blank"
                                class="social-link">SoundCloud</a
                            >
                            <a
                                href="https://www.instagram.com/f.luindo/"
                                target="_blank"
                                class="social-link">Instagram</a
                            >
                        </div>
                    </div>
                </section>
            </section>

            <style>
                :root {
                    --sky-blue: #87ceeb;
                    --sky-light: #3b82f6;
                    --sky-bright: #60a5fa;
                    --sky-pale: #dbeafe;
                    --text-white: #ffffff;
                    --text-black: #000000;
                    --text-gray: #6b7280;
                }

                * {
                    margin: 0;
                    padding: 0;
                    box-sizing: border-box;
                }

                html {
                    scroll-behavior: smooth;
                }

                body {
                    font-family:
                        "Inter",
                        -apple-system,
                        BlinkMacSystemFont,
                        sans-serif;
                    background: var(--sky-blue);
                    color: var(--text-white);
                    overflow-x: hidden;
                    line-height: 1.2;
                    font-weight: 700;
                    margin: 0;
                    padding: 0;
                    width: 100%;
                    max-width: 100vw;
                }

                /* Remove fluid background */
                .fluid-bg {
                    display: none;
                }

                .epk-container {
                    position: relative;
                    z-index: 1;
                    width: 100%;
                    max-width: 100vw;
                    overflow-x: hidden;
                }

                /* Typography */
                h1,
                h2,
                h3,
                h4 {
                    font-family: "Inter", sans-serif;
                    line-height: 1;
                    color: var(--text-white);
                    font-weight: 900;
                    text-transform: uppercase;
                    letter-spacing: -0.02em;
                }

                /* Hero Section */
                .hero-section {
                    min-height: 60vh;
                    display: flex;
                    align-items: center;
                    padding: 4rem;
                    position: relative;
                    width: 100%;
                    max-width: 100vw;
                    box-sizing: border-box;
                    background: var(--sky-light);
                    overflow: hidden;
                }

                .hero-background {
                    position: absolute;
                    top: 0;
                    right: 0;
                    width: 50%;
                    height: 100%;
                    z-index: 1;
                }

                .hero-image {
                    width: 100%;
                    height: 100%;
                    object-fit: cover;
                    opacity: 0.3;
                }

                /* Mobile hero image - full width */
                @media (max-width: 768px) {
                    .hero-background {
                        width: 100%;
                        left: 0;
                        right: 0;
                    }
                }

                .hero-content {
                    width: 100%;
                    max-width: 1200px;
                    position: relative;
                    z-index: 2;
                }

                .hero-text {
                    margin-bottom: 2rem;
                }

                .artist-name {
                    font-size: clamp(4rem, 12vw, 12rem);
                    font-weight: 900;
                    letter-spacing: -0.05em;
                    margin-bottom: 2rem;
                    color: var(--text-white);
                    font-family: "Inter", sans-serif;
                    line-height: 0.8;
                    text-transform: uppercase;
                    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
                }

                .tagline {
                    font-size: clamp(0.875rem, 2vw, 1.125rem);
                    font-weight: 600;
                    letter-spacing: 0.1em;
                    margin-bottom: 1rem;
                    color: var(--text-white);
                    font-family: "Inter", sans-serif;
                    text-transform: uppercase;
                }

                .subtitle {
                    font-size: clamp(1rem, 2.5vw, 1.25rem);
                    font-weight: 400;
                    color: var(--text-white);
                    font-style: italic;
                    text-transform: none;
                    letter-spacing: 0;
                    opacity: 0.9;
                }

                .scroll-hint {
                    position: absolute;
                    bottom: 2rem;
                    left: 4rem;
                    display: flex;
                    flex-direction: column;
                    color: var(--text-gray);
                    font-size: 0.75rem;
                    letter-spacing: 0.2em;
                    text-transform: uppercase;
                    font-weight: 600;
                }

                .scroll-line {
                    width: 2px;
                    height: 4rem;
                    background: var(--text-black);
                    margin-top: 1rem;
                    animation: scroll-pulse 2s ease-in-out infinite;
                }

                @keyframes scroll-pulse {
                    0%,
                    100% {
                        opacity: 0.3;
                    }
                    50% {
                        opacity: 1;
                    }
                }

                /* Section Styles */
                section {
                    padding: 4rem 4rem;
                    background: var(--text-white);
                    color: var(--text-black);
                    width: 100%;
                    max-width: 100vw;
                    box-sizing: border-box;
                }

                .section-header {
                    margin-bottom: 4rem;
                }

                .section-title {
                    font-size: clamp(3rem, 8vw, 8rem);
                    font-weight: 900;
                    color: var(--text-black);
                    margin-bottom: 2rem;
                    line-height: 0.8;
                    text-transform: uppercase;
                    letter-spacing: -0.03em;
                }

                .section-line {
                    width: 120px;
                    height: 4px;
                    background: var(--sky-blue);
                    margin: 0;
                }

                /* Bio Section */
                .bio-content {
                    max-width: 900px;
                }

                .bio-content p {
                    font-size: clamp(1.25rem, 3vw, 2rem);
                    color: var(--text-black);
                    line-height: 1.3;
                    font-weight: 500;
                    margin-top: 2rem;
                }

                /* Music Section */
                .music-section {
                    background: var(--sky-light);
                    color: var(--text-white);
                }

                .music-section .section-title {
                    color: var(--text-white);
                }

                .music-section .section-line {
                    background: var(--text-white);
                }

                .spotify-embed {
                    margin: 4rem 0;
                    max-width: 600px;
                }

                .platform-links {
                    display: flex;
                    flex-direction: column;
                    gap: 1rem;
                    margin-top: 4rem;
                }

                .platform-link {
                    color: var(--text-white);
                    text-decoration: none;
                    font-weight: 900;
                    font-size: 1.2rem;
                    padding: 1rem;
                    border: 2px solid var(--text-white);
                    text-transform: uppercase;
                    letter-spacing: 0.1em;
                    background: transparent;
                    text-align: center;
                    border-radius: 4px;
                    transition: all 0.2s ease;
                }

                .platform-link:hover {
                    background: var(--text-white);
                    color: var(--sky-blue);
                }

                /* Performance Section */
                .performance-content {
                    margin-top: 3rem;
                }

                .performance-types {
                    display: grid;
                    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
                    gap: 2rem;
                    margin-bottom: 4rem;
                }

                .performance-card {
                    background: rgba(255, 255, 255, 0.5);
                    backdrop-filter: blur(5px);
                    border-radius: 16px;
                    padding: 2.5rem;
                    text-align: center;
                    border: 1px solid rgba(122, 181, 195, 0.3);
                    transition: all 0.3s ease;
                }

                .performance-card:hover {
                    transform: translateY(-6px);
                    box-shadow: 0 15px 40px rgba(44, 95, 107, 0.2);
                }

                .performance-icon {
                    margin-bottom: 1.5rem;
                }

                .nature-icon {
                    font-size: 3rem;
                    opacity: 0.8;
                }

                .performance-card h3 {
                    font-size: 1.5rem;
                    color: var(--ocean-deep);
                    margin-bottom: 1rem;
                }

                .performance-card p {
                    color: var(--text-medium);
                    line-height: 1.7;
                }

                .collective-info {
                    background: linear-gradient(
                        135deg,
                        rgba(212, 196, 160, 0.3),
                        rgba(168, 211, 219, 0.3)
                    );
                    border-radius: 20px;
                    padding: 3rem;
                    text-align: center;
                    border: 1px solid rgba(139, 115, 85, 0.3);
                }

                .collective-info h3 {
                    font-size: 2rem;
                    color: var(--earth-dark);
                    margin-bottom: 1.5rem;
                }

                .collective-info p {
                    font-size: 1.125rem;
                    color: var(--text-dark);
                    line-height: 1.8;
                    max-width: 700px;
                    margin: 0 auto;
                }

                /* Contact Section */
                .contact-intro {
                    font-size: clamp(1.25rem, 3vw, 1.75rem);
                    color: var(--text-black);
                    margin-bottom: 4rem;
                    font-weight: 500;
                }

                .contact-grid {
                    display: grid;
                    grid-template-columns: 1fr 1fr;
                    gap: 6rem;
                    margin-bottom: 6rem;
                }

                .contact-card h3 {
                    font-size: clamp(2rem, 4vw, 3rem);
                    color: var(--text-black);
                    margin-bottom: 1rem;
                    font-weight: 900;
                    text-transform: uppercase;
                    letter-spacing: -0.02em;
                }

                .contact-link {
                    color: var(--sky-blue);
                    text-decoration: none;
                    font-weight: 700;
                    font-size: clamp(1.125rem, 2.5vw, 1.5rem);
                    display: block;
                    margin-bottom: 1rem;
                    transition: opacity 0.2s ease;
                }

                .contact-link:hover {
                    opacity: 0.7;
                }

                .contact-card p {
                    color: var(--text-gray);
                    font-size: clamp(0.9rem, 2vw, 1.125rem);
                    line-height: 1.4;
                    font-weight: 400;
                }

                        .social-platforms {
            display: flex;
            flex-direction: column;
            gap: 1rem;
            margin-top: 6rem;
        }

        .social-link {
            color: var(--sky-blue);
            text-decoration: none;
            font-weight: 900;
            font-size: 1.2rem;
            padding: 1rem;
            border: 2px solid var(--sky-blue);
            text-transform: uppercase;
            letter-spacing: 0.1em;
            background: transparent;
            text-align: center;
            border-radius: 4px;
            transition: all 0.2s ease;
        }

        .social-link:hover {
            background: var(--sky-blue);
            color: var(--text-white);
        }

                /* Responsive Design */
                @media (max-width: 768px) {
                    section {
                        padding: 3rem 1.5rem;
                    }

                    .hero-section {
                        padding: 2rem 1.5rem;
                        min-height: 100vh;
                    }

                    .artist-name {
                        font-size: clamp(3rem, 15vw, 6rem);
                    }

                    .section-title {
                        font-size: clamp(2rem, 10vw, 4rem);
                    }

                    .contact-grid {
                        grid-template-columns: 1fr;
                        gap: 3rem;
                    }

                    

                    .social-link {
                        border-color: var(--text-black);
                    }

                    .social-link:last-child {
                        border-bottom: 3px solid var(--text-black);
                    }

                    .scroll-hint {
                        left: 1.5rem;
                    }
                }

                @media (max-width: 480px) {
                    section {
                        padding: 2rem 1rem;
                    }

                    .hero-section {
                        padding: 1.5rem 1rem;
                    }

                    .hero-content {
                        margin-bottom: 2rem;
                    }

                    .tagline {
                        font-size: 0.9rem;
                    }

                    .subtitle {
                        font-size: 1rem;
                    }

                    .performance-card,
                    .contact-card,
                    .track-card {
                        padding: 1.5rem;
                    }

                    .collective-info {
                        padding: 1.5rem;
                    }

                    .collective-info h3 {
                        font-size: 1.5rem;
                    }

                    .contact-intro {
                        font-size: 1.125rem;
                    }

                    .scroll-hint {
                        left: 1rem;
                    }
                }

                /* Very small screens */
                @media (max-width: 320px) {
                    section {
                        padding: 1.5rem 0.75rem;
                    }

                    .hero-section {
                        padding: 1rem 0.75rem;
                    }

                    .artist-name {
                        font-size: clamp(2.5rem, 20vw, 4rem);
                    }

                    .section-title {
                        font-size: clamp(1.5rem, 12vw, 3rem);
                    }

                    .platform-link,
                    .social-link {
                        padding: 1rem 1.5rem;
                        font-size: 0.9rem;
                    }

                    .scroll-hint {
                        left: 0.75rem;
                    }
                }

                /* Additional mobile overflow fixes */
                @media (max-width: 768px) {
                    * {
                        max-width: 100%;
                    }

                    .hero-content,
                    .section-header,
                    .bio-content,
                    .music-content,
                    .contact-content {
                        width: 100%;
                        max-width: 100%;
                        box-sizing: border-box;
                    }

                    .artist-name {
                        word-break: break-word;
                        hyphens: auto;
                    }

                    .section-title {
                        word-break: break-word;
                        hyphens: auto;
                    }
                }

                /* Smooth scroll behavior */
                @media (prefers-reduced-motion: no-preference) {
                    html {
                        scroll-behavior: smooth;
                    }
                }
            </style>
        </div>
    </body>
</html>
